@using ElsaMina.Core.Services.Templates
@using ElsaMina.Core.Utils
@inherits ElsaMina.Core.Services.Templates.LocalizableTemplatePage<ElsaMina.Commands.ConnectFour.ConnectFourLeaderboardViewModel>
@{
    const string headerStyle = $"border-bottom: 1px solid {StyleConstants.PRIMARY_BORDER_COLOR}; padding: 4px";
}
<div style="max-height: 200px; overflow-y: auto">
    <table style="margin: auto; border: 1px solid @StyleConstants.PRIMARY_BORDER_COLOR; border-radius: 12px; padding: 4px">
        <thead>
        <tr>
            <th colspan="6" scope="colgroup">@GetString("c4_leaderboard_title")</th>
        </tr>
        <tr>
            <th scope="col" style="@headerStyle">#</th>
            <th scope="col" style="@headerStyle">@GetString("leaderboard_user")</th>
            <th scope="col" style="@headerStyle">@GetString("c4_leaderboard_rating")</th>
            <th scope="col" style="@headerStyle">@GetString("c4_leaderboard_wins")</th>
            <th scope="col" style="@headerStyle">@GetString("c4_leaderboard_losses")</th>
            <th scope="col" style="@headerStyle">@GetString("c4_leaderboard_draws")</th>
        </tr>
        </thead>
        <tbody>
        @for (int i = 0; i < Model.Leaderboard.Count; i++)
        {
            var entry = Model.Leaderboard[i];
            <tr>
                <td style="text-align:center">@(i + 1)</td>
                <td style="color: @entry.UserId.ToColorHexCodeWithCustoms()">@entry.UserId</td>
                <td style="text-align:center">@entry.Rating</td>
                <td style="text-align:center">@entry.Wins</td>
                <td style="text-align:center">@entry.Losses</td>
                <td style="text-align:center">@entry.Draws</td>
            </tr>
        }
        </tbody>
    </table>
</div>
