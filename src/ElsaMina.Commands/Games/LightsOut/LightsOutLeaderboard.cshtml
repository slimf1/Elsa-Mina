@using ElsaMina.Core.Services.Templates
@using ElsaMina.Core.Utils
@inherits ElsaMina.Core.Services.Templates.LocalizableTemplatePage<ElsaMina.Commands.Games.LightsOut.LightsOutLeaderboardViewModel>
@{
    const string headerStyle = $"border-bottom: 1px solid {StyleConstants.PRIMARY_BORDER_COLOR}; padding: 4px";
}
<div style="max-height: 200px; overflow-y: auto">
    <table style="margin: auto; border: 1px solid @StyleConstants.PRIMARY_BORDER_COLOR; border-radius: 12px; padding: 4px">
        <thead>
        <tr>
            <th colspan="5" scope="colgroup">@GetString("lo_leaderboard_title")</th>
        </tr>
        <tr>
            <th scope="col" style="@headerStyle">#</th>
            <th scope="col" style="@headerStyle">@GetString("leaderboard_user")</th>
            <th scope="col" style="@headerStyle">@GetString("lo_leaderboard_level")</th>
            <th scope="col" style="@headerStyle">@GetString("lo_leaderboard_stars")</th>
            <th scope="col" style="@headerStyle">@GetString("lo_leaderboard_best_moves")</th>
        </tr>
        </thead>
        <tbody>
        @for (int i = 0; i < Model.Leaderboard.Count; i++)
        {
            var entry = Model.Leaderboard[i];
            <tr>
                <td style="text-align:center">@(i + 1)</td>
                <td style="color: @entry.UserId.ToColorHexCodeWithCustoms()">@entry.UserId</td>
                <td style="text-align:center">@entry.Level</td>
                <td style="text-align:center">@entry.TotalStars ★</td>
                <td style="text-align:center">@entry.BestMoves</td>
            </tr>
        }
        </tbody>
    </table>
</div>
