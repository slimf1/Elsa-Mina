@using ElsaMina.Commands.Teams
@inherits ElsaMina.Core.Templates.LocalizableTemplatePage<ElsaMina.Commands.Teams.TeamListViewModel>

<div
    style="max-height: 210px; overflow-y: auto;">
    @foreach (var team in Model.Teams)
    {
        <div>
            @{ await IncludeAsync("Teams/SampleTeam", new SampleTeamViewModel
               {
                   Culture = Model.Culture,
                   Team = team
               }); }
        </div>
    }
</div>